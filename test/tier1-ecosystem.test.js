import test from'node:test';import assert from'node:assert/strict';import{linkTierOneChildren}from'../src/tournaments/relationships.js';import{classifyTournament,eventType}from'../src/tournaments/eligibility.js';
test('event types are detected',()=>{assert.equal(eventType('Event: Europe Qualifier'),'qualifier');assert.equal(eventType('Event Play-In'),'play-in');assert.equal(eventType('Event Showmatch'),'showmatch');});
test('child event links to Tier 1 parent',()=>{const rows=linkTierOneChildren([{id:'main',name:'Esports Nations Cup 2026',verifiedTierOne:true},{id:'q',name:'Esports Nations Cup 2026: Europe West Qualifier'}]);assert.equal(rows[1].parentTournamentId,'main');assert.equal(rows[1].verifiedTierOneChild,true);});
test('unlinked qualifier is rejected',()=>assert.equal(classifyTournament({name:'Random Regional Qualifier',startDate:'2026-09-01',endDate:'2026-09-02'},{hasSchedule:true}).state,'rejected'));
test('linked qualifier can be upcoming',()=>{const x=classifyTournament({name:'Major: Closed Qualifier',startDate:'2026-09-01',endDate:'2026-09-02',parentTournamentId:'main',verifiedTierOneChild:true},{hasSchedule:true},Date.parse('2026-08-23T12:00:00Z'));assert.equal(x.state,'upcoming');});
