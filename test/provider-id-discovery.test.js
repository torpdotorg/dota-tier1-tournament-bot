import test from'node:test';import assert from'node:assert/strict';import{extractLeagueHints}from'../src/tournaments/providerIdDiscovery.js';import{providerResolutionFor}from'../src/tournaments/providerResolution.js';
test('extracts authoritative OpenDota league link',()=>{const rows=extractLeagueHints(['https://www.opendota.com/leagues/23456'],{name:'Example Cup'});assert.equal(rows[0].leagueId,'23456');assert.equal(rows[0].providerIdAuthoritative,true);});
test('ignores unrelated links',()=>assert.deepEqual(extractLeagueHints(['https://twitch.tv/example'],{name:'Example'}),[]));
test('authoritative links become diagnostic candidates only',()=>{const event={name:'Example Cup'},candidate=extractLeagueHints(['https://www.opendota.com/leagues/23456'],event)[0],result=providerResolutionFor(event,[candidate]);assert.equal(result.providerIdState,'candidate');assert.equal(result.providerIdConfidence,100);});
test('different-year name match stays unresolved',()=>{const event={name:'PGL Wallachia Season 9',startDate:'2026-09-17',endDate:'2026-09-27'},result=providerResolutionFor(event,[{name:event.name,leagueId:'1',startDate:'2025-09-17',endDate:'2025-09-27'}]);assert.equal(result.providerIdState,'unresolved');});
