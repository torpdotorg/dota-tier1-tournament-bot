import test from 'node:test';import assert from'node:assert/strict';import{scoreProviderCandidate,providerResolutionFor}from'../src/tournaments/providerResolution.js';
const event={name:'PGL Wallachia Season 9',startDate:'2026-09-17',endDate:'2026-09-27'};
test('exact name and overlapping dates produce a strong candidate',()=>{const r=providerResolutionFor(event,[{name:'PGL Wallachia Season 9',leagueId:'22001',startDate:'2026-09-18',endDate:'2026-09-27'}]);assert.equal(r.providerIdState,'candidate');assert.equal(r.providerIdCandidate,'22001');assert.ok(r.providerIdConfidence>=85);});
test('same name from another year is not promoted',()=>{const r=providerResolutionFor(event,[{name:'PGL Wallachia Season 9',leagueId:'21001',startDate:'2025-09-17',endDate:'2025-09-27'}]);assert.equal(r.providerIdState,'unresolved');assert.ok(r.providerIdConfidence<85);});
test('ambiguous close candidates remain unresolved',()=>{const rows=[{name:'BLAST SLAM VIII',leagueId:'1',startDate:'2026-09-29',endDate:'2026-10-05'},{name:'BLAST SLAM VIII',leagueId:'2',startDate:'2026-09-29',endDate:'2026-10-05'}];const r=providerResolutionFor({name:'BLAST SLAM VIII',startDate:'2026-09-29',endDate:'2026-10-05'},rows);assert.equal(r.providerIdState,'unresolved');assert.match(r.providerIdReason,/too close/);});
test('scoring exposes evidence',()=>{const r=scoreProviderCandidate(event,{name:event.name,leagueId:'9',startDate:event.startDate,endDate:event.endDate});assert.ok(r.reasons.includes('exact normalized name'));assert.ok(r.reasons.includes('date ranges overlap'));});
